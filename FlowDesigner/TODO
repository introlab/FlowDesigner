document parameters for batch mode

support removing network terminals (implemented, but buggy)

set the isModified flag when adding a net terminal or moving a node, ...

debug thread race with run button disable

implement variable number of inputs/outputs

prevent from running a network that has an input (ie that has to be included
somewhere else)

complete "count" display in probe

implement Matrix class

implement image/matrix probe


